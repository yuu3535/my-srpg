# 自作SRPG 2 — 現行ゲームシステム実装記録

調査日：2026-09-21  
対象：`C:/Users/jade-/Desktop/自作srpg 2` の調査時点の作業ツリー。未コミットの既存変更を含む。  
目的：再設計の前提として、現在のコードの処理と接続状況を記録する。

この文書は理想仕様・採用予定仕様ではない。チャットで検討した因果レベル、兵種育成、デュアル等を、現行機能として扱わない。コード・UI・既存文書は変更していない。

## 読み方と調査範囲

| 区分 | この文書での意味 |
|---|---|
| 実装・接続済み | 処理本体があり、現行ゲームの呼び出し経路につながっている |
| 処理あり・通常利用経路なし／限定 | 処理はあるが、初期所持者・敵AI・現在のUIなどの制約で通常プレイから使えない、または経路が限られる |
| 仮処理 | コマンドや状態追加・ログは動くが、記載された効果の全部または主要部分が実現していない |
| データ定義のみ | 定義や保存フィールドはあるが、それをゲーム効果にする処理が未接続 |
| 設計資料のみ | Markdown等に記述されているが、対応する実行処理を確認できない |
| 未実装 | 調査したゲーム実行コードに対応する仕組みがない |

静的なコード読解を中心に、`characters.js` と `statConversion.js` 等をメモリ上で読み込み、能力値の実計算とデータ一覧を確認した。ブラウザによる全機能の操作検証は行っていない。「不整合」は、特記しない限りコード経路の比較で確認したもの。画面上で再現済みの不具合一覧という意味ではない。

### 実行構成

`index.html` が通常の `<script>` で以下を順に読み込む。ビルドツールを前提としないブラウザ向けJavaScript。

1. `characters.js`
2. `skills.js`
3. `magics.js`
4. `spells.js`
5. `combatArts.js`
6. `passiveSkills.js`
7. `battleHooks.js`
8. `statConversion.js`
9. `dataHelper.js`
10. `battleDefinitions.js`
11. `partyState.js`
12. `scenario.js`
13. `game.js`

`buildMilestones.js` は存在するが、`index.html` では読み込まれない。`map_editor.html` は独立した編集補助画面で、ゲーム実行の一部ではない。`prototypes/` 内のHTMLも本編の実行経路ではない。

## 1. ユニットのデータ構造

主な参照：`characters.js / CHARACTERS_DATA`、`game.js / setBattleMode`、`partyState.js / createPartyState`。

### 1.1 キャラクター定義

`CHARACTERS_DATA` は9体のオブジェクト配列。味方5体、敵4体。

| id | 表示名 | 陣営 | 定義Lv |
|---|---|---|---:|
| `ringholm` | リングホルム | 味方 | 5 |
| `arshe` | アルシェ | 味方 | 4 |
| `albas` | アルバス | 味方 | 5 |
| `young_arshe` | アルシェ | 味方 | 1 |
| `young_karima` | カリマ | 味方 | 1 |
| `forest_guard` | 森の番人 | 敵 | 2 |
| `dylan` | ディラン | 敵 | 4 |
| `gunter` | ギュンター | 敵 | 4 |
| `herel` | ヘレル | 敵 | 4 |

幼アルシェとLv4アルシェは別ID・別定義。成長に伴って一方を他方へ変換したり、所持品や育成状態を共有したりする処理はない。

| データ群 | フィールド | 現在の役割 |
|---|---|---|
| 識別・人物情報 | `id, name, side, race, clan` | 識別、陣営判定、表示。種族・一族による一括した戦闘補正の仕組みはない |
| 配置 | `x, y` | 0始まり。xが列、yが行。戦闘定義の配置が優先 |
| レベル | `level` | 固定成長値の取得、必殺計算、表示に使用 |
| リソース | `hp, maxHp, mp, maxMp` | 定義上の値。戦闘生成時に計算値／保存値で上書き |
| HP成長元 | `stats.hp` | 一部の味方は `{ base, gains }` を保持 |
| 基礎能力 | `str, con, dex, pow, edu, int` | 数値、または `{ base, gains }` |
| 固定的な能力 | `siz, app, courage, luck` | 体格、魅力、勇気、幸運。現定義では数値 |
| 行動性能 | `move, attackRange` | 現在は全9体とも移動3・通常射程1 |
| 行動状態 | `moved, acted` | 移動済み／行動済み |
| 魔法所持 | `spells` | `{ 日本語魔法ID: 数値 }`。例：`火: 9`。戦闘用定義は `SPELLS_DATA` |
| 特技・技術 | `skills` | `{ 日本語技能名: 数値 }`。命中・回避・火力補正や一部特技コマンドで参照 |
| パッシブ所持 | `learnedPassives, equippedPassives` | リングホルムの殺気、ギュンターの忠誠心に設定あり |
| 反撃方針 | `counterMode` | `auto / magic_first / physical_only / none` を処理可能 |
| 状態異常等 | `statusEffects` | 効果オブジェクトの配列。定義では空 |
| 人物設定表示 | `seizureType, secretArt` | 発作・秘伝の名称。名称に対応した発作判定や秘伝発動処理はない |
| 表示情報 | `char, tokenImage, portraitImage, portraitImageDamaged`、各種画像位置・サイズ | コマ、立ち絵、被弾絵、画面表示用。戦闘能力ではない |

現行の9体には `equipment`、`weapon`、`weaponPower`、`weaponFormula`、初期 `items` の実データは設定されていない。これらを受け取る処理や表示枠は別途存在する。

### 1.2 戦闘用ユニット

`setBattleMode` は参加する定義から `battleUnits` を生成する。コメントには「ディープコピー」とあるが、実際はオブジェクト展開と一部フィールドの個別コピー。基礎能力の `{ base, gains }` 等まで全面的に深くコピーする処理ではない。

生成時に設定・更新する値：

- 戦闘定義由来の座標、保存データ由来のレベル・HP・MP・所持品。
- `maxHp = calcBattleStats(...).hp`、`maxMp = calcBattleStats(...).mp`。
- `learnedArts / equippedArts / learnedPassives / equippedPassives / buildChoices`。
- `combatArtUses = {}`：戦闘中の戦技使用回数。
- `moved = false`、`acted = false`、`statusEffects = []`。
- `spells`、`skills` のコピー。
- `physicalBonus = "力○"`、`magicBonus = "魔○"` という文字列。
- `battleStats`：生成時の派生能力。主な戦闘処理は必要時に `calcBattleStats(unit)` を再実行する。

戦闘中には `battleCourageLoss` が悪夢によって追加される。命中等は `attentionBonus`、`criticalBonus`、`criticalAvoidanceBonus`、`equipmentBonus` 等も受け取れるが、現キャラ定義や通常の育成・装備操作からそれらを設定する仕組みはない。

### 1.3 パーティ保存データ

`partyState = { version: 1, members: { [id]: ... } }`。味方のみ登録。

各メンバーのフィールドは `level, hp, mp, items, skillRanks, learnedArts, equippedArts, learnedPassives, equippedPassives, buildChoices`。

ただし **`skillRanks` は保存枠があるだけで、保存値を戦闘ユニットへ渡す経路と戦闘後に更新する経路がない**。一方、実際の `skills` と `spells` は戦闘開始時にキャラ定義からコピーされる。

## 2. ステータス計算式

主な参照：`statConversion.js` 全体、`dataHelper.js / fixedDiceValue, getWeaponPower, getSpellPower`、`game.js / getBattleHitResult, calculatePhysicalDamage, calculateMagicDamage, getContextCriticalRate`。

### 2.1 基礎値の取得

`statAt(stat, level)`：

- 数値ならその数値を返す。
- `{ base, gains }` なら `base + gains の先頭 max(0, level - 1) 個の合計`。
- 不正・未設定の値は0。
- 記録された `gains` を使い切った後は、レベルを上げてもその能力の加算は増えない。

`rawStat(unit, key)` は `unit.stats[key]` があればそちらを優先し、なければ `unit[key]` を使う。`calcBattleStats` 内では、取得後のSIZ・APPが0等の場合に10へ置き換える。

### 2.2 派生能力

| 派生値 | 実際の式・取得元 |
|---|---|
| 最大HP | `statAt(unit.stats.hp ?? unit.maxHp ?? unit.hp, level)` |
| 最大MP | `POW` |
| 物攻 `atk` | `STR` |
| 魔攻 `mag` | `POW` |
| 技量 `skl` | `DEX` |
| 物防 `def` | `floor((STR + SIZ) / 2) + 装備物防補正` |
| 魔防 `res` | `floor((POW + CON) / 2) + 装備魔防補正` |
| 注目度 `attention` | `APP` |
| 補助魔力 `supportMagic` | `floor(POW / 3)` |
| 運の派生値 `luckT` | `floor(luck / 10)`。消費するトークン処理はない |
| 有効勇気 | `max(0, 基礎courage - battleCourageLoss)` |
| 実反撃発生率 | `clamp(有効勇気, 0, 100)` |

装備防御補正は、数値の `equipmentBonus.def / res` を優先し、なければ `equipmentArmor / equipmentWard`。`equipmentBonus.atk` 等を物攻へ一括適用する処理はない。

互換名として `power=atk, magic=mag, technique=skl, armor=def, ward=res, valor=counterRate, luckTokens=luckT` も返す。ここで `calcBattleStats` の `counterRate/valor` は基礎勇気で、悪夢後の反撃率は `getCounterRate` で別計算する。

### 2.3 命中と回避

以下の `round` は `Math.round`、`floor` は `Math.floor`。

```text
体格回避補正 = clamp((11 - SIZ) × 2, -14, 14)
基礎命中 = 50 + DEX
基礎回避 = round(STR × 1.5 + DEX × 0.5 + 体格回避補正)
攻撃精度 = 基礎命中 + 使用技能値 × 5 + 命中補正
回避スコア = round(基礎回避 × (1 + 回避技能値 × 0.05)) + 回避補正
最終命中率 = clamp(round(攻撃精度 - 回避スコア), 20, 95)
```

- 通常は1〜100の乱数が最終命中率以下なら成功。`BATTLE_HIT_MODE = "formula"`。
- 相手がスタン中なら通常攻撃・投擲・攻撃魔法は100%命中になる経路がある。物理反撃はスタン状態を引数へ反映していない。
- 使用技能は選択した物理技能、または使用魔法の `spells[id]` の値。
- 通常の回避技能参照は `skills["回避"] ?? 0`。ステータス変換側だけは `skillRanks["回避"]` へのフォールバックもある。
- `accuracyDown` は状態の `value × 5` を命中へ加算。虚像の `-2` は最終計算上の命中スコア-10、氷の `-1` は-5。
- `evasionUp / evasionBonus` は数値を回避スコアへ加算。
- 殺気は自分から攻撃した際に命中+10、相手回避-10。忠誠心は条件成立時に自分の命中・回避へ各+3。
- 通常の補助・回復魔法は `getMagicHitResult` で100%成功。転移だけは専用経路で「転移技能値×10%」を判定する。

### 2.4 物理・魔法ダメージ

```text
武道補正 = clamp(floor(武道技能値 / 3), 0, 3)
魔導補正 = clamp(floor(魔導技能値 / 3), 0, 3)

物理通常ダメージ =
  max(1, STR + 武器威力 + 戦技加算 + 攻撃側忠誠心 - (物防 + 防御側忠誠心))
  + 武道補正

魔法通常ダメージ =
  max(1, POW + 呪文威力 + 魔法威力補正 + 攻撃側忠誠心 - (魔防 + 防御側忠誠心))
  + 魔導補正

反撃ダメージ = max(1, floor(対応する通常ダメージ / 2))
```

現在、実際に接続された戦技威力加算は両断の+5。武道・魔導は確率発動ではなく常時の固定加算。

武器威力は `unit.weaponPower` の数値、次に `weaponFormula` の固定化値、どちらもなければ3。現在の全キャラは既定値3を使用する。

呪文威力は `powerFormula` から `MB` を取り除き、ダイス部分を切り捨て平均値に固定化する。例：`1d6+MB → 3`、`1d8+MB → 4`、`2d4 → 5`、`1d12+MB → 6`。通常攻撃のダメージにTRPGのDB／MBダイスを振って加える方式ではない。

計算後にアクション／対象別の加算・倍率を適用する共通処理もあるが、現在登録された効果は限定的。必殺・結界の順序は次項のとおり。

### 2.5 必殺・結界・気絶

```text
必殺値 = floor(有効勇気 / 2) + (level - 5) × 5 + 必殺補正
必殺耐性 = 対象APP × 2 + 対象の必殺耐性補正
必殺率 = clamp(floor(必殺値 - 必殺耐性), 0, 95)
必殺ダメージ = floor(通常ダメージ × 3)
```

命中したダメージ処理で別の1d100を振る。幸運はこの必殺式に使わない。殺気は通常攻撃側のコンテキストへ必殺+10を加える。

結界は防御力の永続加算ではなく、`statusEffects` に置いた吸収量。必殺処理後のダメージから吸収する。

- 通常：最初に見つかった結界の `value` を消費し、超過分だけHPへ。吸収だけでHPダメージ0もあり得る。
- 吸収量を使い切ると、コードはその対象の `type === "barrier"` をすべて削除する。
- 破壊魔法：結界の吸収自体は行うが、残量にかかわらず結界をすべて削除する。FE型の武器相性ブレイクとは別物。
- 普通の物理命中・`magicDamage` では、HPが「2超から1〜2」へ減った時にCON×5%判定。失敗するとHP0。反撃・破壊・範囲魔法等の全経路に共通化されてはいない。

### 2.6 移動・射程・その他

- 移動は `unit.move` の固定値。速さ・兵種・体格から算出しない。
- 通常射程は `unit.attackRange`。投擲は縦横直線4マス。魔法射程は `SPELLS_DATA[id].range`。
- 原則の距離はマンハッタン距離 `abs(dx) + abs(dy)`。独立した速さ、追撃閾値、武器重量、攻速は存在しない。
- EDU・INTは `raw` に保持するが、現行の主要戦闘計算で使っていない。
- APPは必殺耐性と敵の狙う優先度に使用。魅力にデュアル機能はまだない。

### 2.7 現行データの計算例

一時効果・位置条件・武器威力を足す前の `calcBattleStats` 等の出力。命中欄は対敵命中率ではない。

| キャラID | Lv | HP | MP | 物攻 | 魔攻 | 物防 | 魔防 | 命中基礎 | 技能込み回避 | 必殺値 | 必殺耐性 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `young_arshe` | 1 | 13 | 18 | 14 | 18 | 13 | 15 | 68 | 34 | 25 | 30 |
| `arshe` | 4 | 22 | 26 | 22 | 26 | 17 | 23 | 73 | 55 | 40 | 30 |
| `ringholm` | 5 | 23 | 28 | 26 | 28 | 18 | 25 | 72 | 75 | 45 | 32 |
| `albas` | 5 | 28 | 36 | 18 | 36 | 16 | 32 | 86 | 37 | 32 | 34 |

## 3. 戦闘処理の順序

主な参照：`game.js / onUnitClick, executePendingBattlePreview, executeAttack, resolvePhysicalHit, executeMagic, endUnitTurn, checkVictoryCondition`。

### 3.1 通常攻撃・両断

1. コマンドから攻撃技能／戦技を選び、`selectedAttackSkill / selectedCombatArtId / actionState` に記録。
2. 敵ユニットをクリック。生存・敵陣営・通常射程を確認。
3. `calculateBattlePrediction` で乱数を振らずに命中率、通常／必殺ダメージ、反撃予測を計算。
4. `_vsAttack` に攻撃者・対象を保持し、予測画面の「実行」で `executePendingBattlePreview → executeAttack`。
5. 攻撃者がスタンなら攻撃を中止して行動終了。
6. 使用技能を決め、対象スタンを含めて命中判定。外れると行動終了。**外れた攻撃には通常反撃判定がない**。
7. 命中時は `resolvePhysicalHit`。対象の魔法カウンター状態を先に分岐判定。
8. 通常経路ではアクションコンテキスト生成、`beforeAttack`、物理ダメージ計算、`beforeDamaged`、加算・倍率、必殺判定、結界消費。
9. HPを減らし、条件を満たす場合は気絶判定。表示・撃破ログを更新。
10. `afterDamage → HPが正から0になればonKill → afterAttack`。
11. 対象が生存し、訓練用パッシブ戦闘ではなく、反撃方針と勇気条件を満たせば反撃発生判定。
12. `endUnitTurn` で移動・行動を両方済みにし、`checkVictoryCondition`。

投擲は `onUnitClick → executeThrow → resolvePhysicalHit`。対象クリックから直接実行され、通常攻撃の確認画面を挟まない。両断等の選択情報も渡さない。

### 3.2 通常反撃

`resolveCounterAttack` が方針を選ぶ。

| 方針 | 実処理 |
|---|---|
| `none` | 反撃しない |
| `physical_only` | 物理反撃 |
| `magic_first` | 候補魔法があれば魔法、なければ物理 |
| `auto` 等 | 半減後の物理・魔法ダメージを比較。魔法が厳密に大きければ魔法。同値なら物理 |

候補魔法は `magicDamage / break` のみ。魔法射程内でMPが1以上なら候補になる。命中率・必殺率・消費MP・結界吸収を含む期待値最大化ではない。

- 反撃は命中判定後、ダメージ半減、必殺判定、結界、HP減少、フックの順。
- 魔法反撃はMPコストを先に消費する。
- 物理反撃に通常射程チェックはない。反撃可否判定にスタンチェックもない。
- 反撃からさらに反撃する処理はない。反撃で `moved / acted` は消費しない。
- `allowMartialArt: false` を渡す箇所はあるが、計算関数はその引数を参照せず、反撃にも武道の固定補正が入る。

### 3.3 魔法「カウンター」

`statusEffects.type === "counter"` は通常反撃とは異なる。

- 通常物理／投擲が命中した際に、算出した必殺込み・結界吸収前ダメージを攻撃者と防御者の双方へ与える。
- 両者の結界を消費・適用しない。
- この分岐では通常の気絶判定・勇気による反撃判定へ進まない。
- 魔法被弾や `executePhysicalCounter / executeMagicCounter` の中では同じ反射分岐を呼ばない。

### 3.4 魔法

1. 現在の横画面魔法メニューは `spells` と `SPELLS_DATA` を照合。
2. `range:null` は選択直後に自身を対象として `executeMagic`。転移は空きマス選択の専用経路。
3. 射程がある魔法は陣営・距離を確認。敵対象は戦闘予測を挟み、味方対象は直接実行。
4. `executeMagic` は攻撃系なら命中を抽選、通常の支援系なら自動成功。
5. 成否にかかわらずMPコストを抽選して減算。足りなければ0に丸める。
6. 成功時、`effectType` ごとにダメージ・回復・状態付与等を実行。
7. ダメージ魔法は必殺・結界・HP減少・対応するフックを処理。詳細は7章。
8. 描画更新、行動終了、勝敗判定。

**自発的な魔法使用後に、相手の勇気による通常反撃を呼ぶ処理はない。** 敵対象の魔法予測画面には反撃が表示され得るが、実処理とは一致しない。

### 3.5 撃破・戦闘後

HPは0未満にならない。HP0ユニットは `battleUnits` から削除せず、描画・移動占有・生存者抽出から除く。撃破経験値、戦利品抽選、成長判定はない。

シナリオ戦闘の勝利時だけパーティの状態を更新し、約2秒後に会話へ戻る。敗北時はパーティへ反映しない。勝敗は9章、保持範囲は10章参照。

### 3.6 フックの実装範囲

`battleHooks.js` は `beforeAttack, beforeDamaged, afterDamage, afterAttack, onKill` の登録・実行機構を持つ。アクションには攻撃者、対象群、魔法／反撃／予測のフラグ、補正、対象別結果、発動済みID等を保持する。

`game.js` で現在登録されるのは `beforeAttack` の両断・絶影・殺気・忠誠心の4つ。定義ファイルの全パッシブを自動解釈する汎用エンジンではない。`modifyPrediction` は登録可能なフック名の一覧にも含まれず、忠誠心は専用関数で処理される。

範囲魔法はアクションコンテキストを共有する一方、`finishDamageHooks` は対象ごとに呼ぶため、`afterAttack` も対象ごとに呼ばれる。`oncePerAction` を指定した登録効果は発動済みIDで重複抑止する。

## 4. ターン・行動システム

主な参照：`game.js / setBattleMode, selectUnit, getMoveRange, moveUnit, endUnitTurn, startEnemyPhase, startAllyPhase, tickStatusEffects`。

- 味方フェーズ→敵フェーズの交互制。開始はターン1・味方フェーズ。
- 次の味方フェーズ開始時に `turnCount++`。
- 各陣営のフェーズ開始時、生存する同陣営ユニットの `moved / acted` を解除。
- 味方は順番を任意に選ぶ。生存味方が全員 `moved && acted` になると、約700ms後に敵フェーズを予約する。
- 敵は配列順に1体ずつ行動する。速さ順ではない。
- 移動は `moved=true` にするだけで、行動を残す。
- 攻撃・魔法・特技・アイテム・待機は原則 `endUnitTurn` へ進み、`moved=true, acted=true`。攻撃後の通常再移動はない。
- 現在の横画面ではユニット選択後に移動先を表示する。別途「移動」ボタンを必ず押す方式ではない。

### 移動と取消

`getMoveRange` は上下左右のBFS。1マス当たりコスト1。敵のいるマス・壁・voidは通れず、味方のマスは通過できるが止まれない。

`moveUnit` は到着マスのアイテムを拾う。`pendingMoveUndo` に直前の移動を保持し、行動前・同ユニット・味方フェーズなら戻せる。拾ったアイテムも盤面へ戻す。複数手・戦闘結果・乱数まで戻す履歴機構はない。

### 再行動・特殊移動

- 加速は対象の `moved=false, acted=false`。使用者自身は直後に `endUnitTurn` されるため、自分に使っても最終的には行動終了する。
- 転移成功は座標変更と `moved=true` のみで、未行動ならコマンドを再表示。失敗は行動終了。
- 転移の選択・実行には「既に移動したか」「MPが足りるか」の禁止条件がない。未行動のまま転移を再選択できる経路がある。

### 状態効果の時間

そのユニット側のフェーズ開始時に `tickStatusEffects(side)`。

1. 火傷は1d3ダメージ、重力は `value`（通常1）ダメージ。
2. スタンはCON×3%で回復判定。
3. 残った状態の `duration` を1減らし、0以下なら削除。

したがって表示上の「1ターン」は必ずしも1ラウンドではない。敵に付与した持続1の状態は、直後の敵フェーズ開始で切れる。絶影は味方が使えば次の味方フェーズ開始まで残る。

スタンで全コマンドが統一的に禁止される構造ではない。通常攻撃では禁止されるが、味方の移動・魔法・投擲・特技の各経路には同じチェックがない。

## 5. レベル・経験値・成長

主な参照：`characters.js`、`statConversion.js / statAt, rawStat, calcBattleStats`、`partyState.js`。

| 要素 | 現在の状態 |
|---|---|
| キャラクターレベル | `level` が存在。定義または保存データから取得 |
| レベル別能力 | 一部キャラの `{ base, gains }` をレベルに応じて集計 |
| 経験値 | 獲得・蓄積・必要量の処理なし |
| 自動レベルアップ | 戦闘や章クリアから `level` を増加させる処理なし |
| 確率成長率・ピン | 未実装 |
| 因果Lv／兵種Lv | データ・進行処理とも未実装 |
| クラス／兵種・転職 | 未実装。種族・一族表示はあるが兵種ではない |
| 技能熟練の上昇 | `skills / spells` は設定値。行動で伸ばす処理なし |
| 修練分岐・振り直し | `buildChoices` 等の保存枠と別ファイルの定義はあるが選択処理なし |
| Lv上限・能力上限 | 育成進行を制御する上限処理なし。因果Lv99という仕様は現行コードにない |

「固定の成長配列がある」ことと「プレイ中に成長できる」ことは別。現状は前者のみ。TRPG資料のレベル別獲得経験値や変異時能力上限も、現行SRPGの経験値・能力キャップには接続されていない。

## 6. 武器システム

主な参照：`dataHelper.js / getWeaponPower`、`game.js / ATTACK_SKILL_PRIORITY, getAttackSkillVal, showAttackRadial, renderLandscapeSubCommandRail, highlightThrowRange`。

- 武器マスター・武器IDによる装備管理・装備交換・売買・鍛錬はない。
- 現在の「攻撃の種類」は主に技能名。優先順は `武器 → 爪 → ブレス → キック → こぶし → 尾撃 → 噛む → 暗器 → 投擲`。
- `getAttackSkillVal` は最初に該当する技能を採用し、次に名前の部分一致を探す。なければ「攻撃」技能値5。
- UIから技能を選び直せるが、投擲以外は基本的に命中に使う技能値が変わるだけ。爪・ブレス等で個別の威力式や攻撃属性を分けていない。
- 通常射程はユニット側の1。投擲は直線4。弓や槍などの専用射程・最低射程はない。
- 通常の威力は前述の既定値3。武器固有命中値・重量・追撃・特効・武器相性・耐久値はない。
- `COMBAT_ARTS` の `cost.durability` はデータのみで、両断の耐久3も消費されない。
- 装備名のUI表示はあるが、表示された名前から `getWeaponPower` が威力を取得する接続はない。
- 原作の双剣の2回攻撃、速さによる追撃、最大4ヒット等の処理はない。通常攻撃は1回の命中とダメージ処理。

投擲ハイライトは壁・void・途中のユニットで止まる。一方、対象クリック時の判定は「縦横直線かつ距離4以内」のみで、途中の遮蔽物を再確認しない。したがって表示される投擲可能マスと実行可能な対象の条件が一致していない。

## 7. 魔法・戦技・スキル

### 7.1 データの役割分担

| ファイル・データ | 接続状況 |
|---|---|
| `spells.js / SPELLS_DATA` | 現行戦闘が参照する魔法30件。射程・MP式・効果種別等 |
| `magics.js / MAGICS` | TRPG寄りの説明、習得条件、EDU対抗、習得時間等。現行の魔法実行はこの定義を使わない |
| `skills.js / SKILLS` | 特技の説明・初期値・一族条件等。説明文を自動的に効果へ変換する処理はない |
| `dataHelper.js / getMagicData, getSkillData, getCharacterSheet` | 上記説明データとの結合ヘルパー。現行 `game.js` から呼ばれていない。魔法側は `char.magics` を参照するが、現キャラ定義は `spells` |
| `combatArts.js / COMBAT_ARTS` | 戦技16件。実装許可リストにある2件だけ使用処理につながる |
| `passiveSkills.js / PASSIVE_SKILLS` | パッシブ14件。実装許可リストにある2件だけ有効 |
| `buildMilestones.js / BUILD_MILESTONES, MAGIC_MASTERY` | 修練トラック・習得条件のデータのみ。ブラウザで未読込 |

### 7.2 魔法のコスト・習得

- 初期所持は `characters.js / spells`。ゲーム中の習得・着脱・ポイント配分画面はない。
- 通常使用はMP不足でも選べ、処理時に `max(0, mp - rollDice(mpCost))`。MP0でも使用を止める判定がない。
- 攻撃魔法が外れてもMPを消費する。
- 魔法反撃の選択時だけ `mp > 0` が必要。ただし抽選されたコストを支払えるかは調べない。
- `mpCost: "任意"` は数値入力を要求せず、`rollDice` では0になる。
- マップごとの魔法使用回数制はない。MP自然回復・全快処理もない。

### 7.3 魔法別の実装状況

「通常味方未所持」は初期定義上の状態。処理があることと、現行AIがその魔法を自発的に使うことは区別する。

| 魔法 | 射程／MP | 実処理・接続範囲 |
|---|---|---|
| 火 | 2／1d6 | 単体魔法ダメージ。結界前・必殺込みダメージ10以上なら火傷1d6持続を確定付与。幸運の追加判定なし |
| 水 | 2／1d6 | 単体魔法ダメージ。追加効果なし。所持者は敵ギュンターのみで、訓練では敵行動・通常反撃が停止 |
| 風 | 2／1d6 | ダメージ処理あり。10以上の吹き飛ばしはログのみで座標変更なし。現キャラ未所持 |
| 土 | 2／1d6 | ダメージ処理あり。移動封じなし。現キャラ未所持 |
| 氷 | 2／1d6 | ダメージ処理あり。10以上で `accuracyDown=-1` を持続3で付与。現キャラ未所持 |
| 落雷 | 3／1d6 | 単体魔法ダメージ。基礎呪文威力4 |
| 治癒・生命 | 2／1d6 | `floor(POW/3) + 呪文威力3` のHP回復。最大HPまで。MP回復・戦闘不能者を選ぶUIはない |
| 加速 | 2／1d6 | 対象の行動・移動フラグを解除。自分対象は行動終了で打ち消される |
| カウンター | 自身／1d6 | 持続2d3の反射状態。実際の反射範囲は3.3節 |
| 虚像 | 2／1d8 | `accuracyDown=-2` を追加。持続式 `1d3+MB` の評価には不整合あり |
| 結界 | 2／1d6 | 吸収量5の結界を追加。持続式 `1d3+MB` の評価には不整合あり |
| 破壊 | 2／1d6 | 魔法ダメージと結界完全除去。吸収分を除いた超過ダメージがHPへ |
| 封印 | 2／1d8 | 持続 `1d3+1` のスタンを追加。効果時間と行動禁止範囲に注意 |
| 悪夢 | 2／1d6 | `1d6 + floor(POW/3)` を `battleCourageLoss` に累積。MP減少・発狂判定なし。現キャラ未所持 |
| 補助 | 2／1d6 | `support, value:1, duration:3` の状態を追加。ただし命中や技能判定への加算が未接続 |
| 転移 | 空きマス5／1d8 | 専用の `executeTeleport` で本人が瞬間移動。成功率は転移技能×10%。通常 `executeMagic` 内の同名分岐はログのみ |
| 変身 | 2／1d8 | 成功ログとMP消費のみ。能力・姿・兵種の変更なし |
| 付与 | 2／任意 | ログのみ。HP/MP移譲なし。現キャラ未所持 |
| 浮遊 | 自身／1d6 | ログのみ。飛行移動や状態付与なし。所持者は敵ヘレルのみ |
| 分霊 | 自身／1d6 | ログのみ。分身生成・リソース共有なし。所持者は敵のみ |
| 隕石 | 対象選択3／1d8 | 範囲ダメージ処理あり。対象範囲は術者から `abs(y差)≤3` かつ `abs(x差)≤1` の敵。所持者は敵ヘレルのみでAI自発使用はない |
| 重力 | 自身／1d10 | 自分以外の生存ユニット全員に毎フェーズ開始1ダメージの状態。所持者は敵ヘレルのみでAI自発使用はない。持続式 `1d6+MB` に不整合あり |
| ヒトダマ | 自身／1d4 | ログのみ。ユニット生成なし |
| 暗器召喚 | 自身／1d2 | ログのみ。武器・所持品生成なし。現キャラ未所持 |
| グレムリン | 自身／1d2 | ログのみ。現キャラ未所持 |
| フギン＆ムギン | 自身／1d2 | ログのみ。現キャラ未所持 |
| ジズ | 自身／5d3 | ログのみ |
| バハムート | 自身／3d3 | ログのみ |
| レヴィア | 自身／4d3 | ログのみ |

隕石は最初に選んだ対象への命中判定を1回行い、それが成功すると術者基準の範囲内全員へダメージを処理する。各対象への命中を再抽選しない。必殺は対象ごとに判定する。指定対象を中心に爆発する実装ではない。

`rollDice` はMBを自動解決しない。結界・虚像・重力の持続式をそのまま渡すと、MB除去後に末尾の `+` が残り、評価エラーを捕捉して0を返す。状態自体は追加されるが次の対象側フェーズ開始で消える。重力は持続を減らす前にダメージを与えるため、持続0でもその時に1回分処理される。悪夢は `evalDamage` でMBを数値置換するので別経路。

### 7.4 戦技

| 戦技 | 区分 | 現在の処理 |
|---|---|---|
| 両断 `ryoudan` | 実装・接続済み | 物理攻撃値+5。耐久コスト3の定義は未消費。回数上限なし |
| 絶影 `zetsuei` | 処理あり・UI経路限定 | 自身の回避スコア+20、持続1、1戦闘3回。使用は行動終了。UIの制約は11章 |
| 円舞 `enbu` | データ定義のみ | 隣接全敵攻撃 |
| 上弦・下弦 `jougen / kagen` | データ定義のみ | 上／下方向3マスへの攻撃 |
| 無影 `muei` | データ定義のみ | 物理射程+2 |
| 死神 `shinigami` | データ定義のみ | 撃破回復・周囲勇気半減 |
| 魔光 `makou` | データ定義のみ | 魔法範囲化・MP3倍 |
| 威光 `ikou` | データ定義のみ | 魔法射程+5・MP3倍 |
| 明星 `myoujou` | データ定義のみ | 撃破回復・周囲回復 |
| 軍略伝授・軍神 | データ定義のみ | 味方全体の能力補正 |
| 煉獄・激流・砂地獄・鎌風 | データ定義のみ | 属性魔法の追加効果 |

利用可能な戦技は `learnedArts` と `equippedArts` の和集合に、実装済み戦技の対応技能値6以上による自動候補追加を行って求める。技能は `skillRanks[track]` を優先し、なければ `skills[track]`。

これは `BUILD_MILESTONES` の分岐選択を実行した結果ではない。修練6以上なら両断・絶影が候補となる簡易処理。装備枠数の制限もなく、習得済みでも未装備だから使えないという分離はない。

### 7.5 パッシブ

| パッシブ | 現在の処理 |
|---|---|
| 殺気 `sakki` | リングホルムに設定。反撃以外で命中+10、相手回避-10、必殺+10。敵反撃を封じる効果はない |
| 忠誠心 `chuuseishin` | ギュンターに設定。2マス以内に別の生存同陣営ユニットがいれば、物理／魔法の攻撃・防御計算、命中・回避に+3。HP・MP・移動・勇気・必殺全般へ+3する処理ではない |
| 生存本能・残心 | データ定義のみ |
| 魔法耐性・詠唱短縮 | データ定義のみ |
| 力の陣・防御の陣・魔力の陣・魔防の陣・急所の陣・回避の陣 | データ定義のみ |
| 回復・魔の鼓動 | データ定義のみ |

`learnedPassives` と `equippedPassives` の和集合から実装許可リストのものを有効化する。習得／装備の区別による無効化や枠数制限はない。殺気のデータ上の `condition.owner` 等を汎用的に評価する処理もない。

忠誠心の所持者ギュンターは、現在の訓練定義では敵側1体だけなので距離条件を満たさない。

### 7.6 特技コマンド

`BATTLE_UTILITY_SKILLS` は応急手当、医学、戦闘指揮、目星、集中、挑発、庇う。`executeSkill` は技能値×10%で1d100判定を行い、成功・失敗どちらでも行動終了する。

- 応急手当・医学：使用者自身のHPを1d3回復。対象選択・蘇生はない。
- 戦闘指揮：「味方全員の命中+1」と表示するが、補正・状態を付与しない。
- 目星・集中・挑発・庇う：成功メッセージのみ。索敵・集中補正・敵注目度変更・かばう処理はない。
- その他の多くの技能：数値を表示するが、対応する戦闘・探索コマンドはない。

## 8. 敵AI

主な参照：`game.js / chooseEnemyTarget, planEnemyActions, renderDeclarations, executeDeclaredAction, enemyAction, startEnemyPhase`。

### 対象と移動先

敵は生存味方の次のスコアが最小の者を選ぶ。

```text
優先度スコア = 距離 × 10 - APP - attentionBonus
```

APPが高いほど狙われやすい。同点なら配列順で先の対象が残る。HPの少なさ・撃破可能性・相性・予測被害による対象評価はない。

通常射程外なら `getMoveRange` の候補から、対象とのマンハッタン距離が最も小さくなる地点へ移動。今より近づけなければ移動しない。遠回りでいったん対象から離れるような長期経路探索は行わない。

### 行動予告

`DECLARATION_MODE=true`。戦闘開始時と味方フェーズ開始時に予告を作る。

- 予告種別は `attack / move / wait / stun`。
- 攻撃予告は `dest, targetId, targetName` を保持。攻撃対象はマス座標ではなくユニットID。
- 予定移動先の重複を避ける `reserved` がある。
- UIは予定移動と対象への線、対象強調等を描く。
- 味方が移動した都度、全予告を最初から作り直す処理ではない。

### 実行時の再判定

攻撃予告では、対象が生きていれば同じIDを追う。対象が倒れていれば生存味方から再選択する。対象の現在位置を使って到達候補を再計算するため、**予告時の移動先に固定されない**。

移動先が塞がれていれば移動しない。移動後、対象が通常射程内なら物理攻撃。届かなければ攻撃しない。`move` だけの予告は移動して終了し、移動後に新たに攻撃へ切り替える処理はない。

予告がある場合はその実行が現在のスタン確認より先。予告作成後に付いたスタンを攻撃予告実行時に再確認しない。逆に `stun` 予告は、敵フェーズ開始時に状態が治っていてもその予告のまま行動を休む。

### AIが使う攻撃

自発行動は通常の物理攻撃のみ。魔法・戦技・特技・アイテム・回復を選ぶAIはない。所持魔法は通常反撃の候補として使われる場合がある。

`battle_tutorial.passive=true` の場合、敵は移動・攻撃をせず、被弾後の勇気反撃も停止する。

## 9. マップ・地形・勝敗

主な参照：`battleDefinitions.js / BATTLE_DEFINITIONS`、`game.js / createGrid, isTileBlocked, getMoveRange, isBattleConditionMet, checkVictoryCondition`。

### マスと地形

- 四角いグリッド。列数・行数は戦闘定義から取得。
- タイルデータは `{ x, y, type }`。`wall` は壁、`void` は存在しないマスとして扱う。
- 地形ごとの守備・回避・魔防補正、森林・沼・炎等の移動コスト、高低差、兵種別通行条件はない。
- 通常移動の各辺のコストは1。
- 通常攻撃・魔法に、射線上の壁を調べる一般的な見通し判定はない。射程ハイライトで対象マスを除外する処理と、途中の壁を調べる処理は別。
- 転移表示は壁・voidを明示的には除外しないが、それらのセルには通常クリックハンドラがない。したがってハイライトとクリック可否が一致しない場合がある。

### 登録済み戦闘

| battleId | サイズ | 参加者 | 特徴 |
|---|---|---|---|
| `battle_tutorial` | 7列×8行 | 幼アルシェ、幼カリマ、ギュンター | 敵行動停止。座標(3,4)にHP5回復のポーション小 |
| `battle_ch1` | 12列×8行 | リングホルム、アルシェ、アルバス、森の番人、ディラン、ヘレル | 通常の敵AI |

両方とも `tiles: []`。背景が森でも森林地形効果はない。個別の初期座標は `BATTLE_DEFINITIONS.positions` に設定。

### 勝利・敗北

- 実装されている条件型は `defeatAll`（敵全滅）と `allAlliesDefeated`（味方全滅）のみ。
- 両戦闘ともその組み合わせを使用。
- 指定ユニット撃破・主人公死亡・防衛・到達・脱出・ターン上限はない。
- `checkVictoryCondition` は勝利を先に判定する。両軍同時全滅なら現コードでは勝利側に入る。
- 火傷等を処理する `tickStatusEffects` の直後には勝敗判定を呼ばない。継続ダメージだけで最後のユニットが倒れた場合、勝敗確定がその場で行われない経路がある。

### マップエディタ

`map_editor.html` は空き・壁・voidを編集し、`cols, rows, tiles` の貼付用テキストを出力する。`updateOutput / copyOutput` を持つ。ゲームが編集結果を自動読み込みする接続はない。

## 10. 戦闘間の状態保持

主な参照：`partyState.js`、`game.js / startBattleSession, setBattleMode, checkVictoryCondition, saveGame, loadGame`。

| 項目 | シナリオ戦闘間の扱い |
|---|---|
| HP | 勝利時に残量を保存。次戦闘では最大値以内へ丸め、0なら暫定的に1で開始 |
| MP | 勝利時の残量を保存。次戦闘でも持ち越す |
| 所持品 | 拾得・使用後の配列を保存 |
| レベル | 保存するが、現行ゲーム内で増加する処理はない |
| 戦技／パッシブ習得・装備リスト | 保存・復元する |
| `buildChoices` | 保存・復元するが、効果計算と選択UIはない |
| `skillRanks` | 保存データ構造に存在するが、戦闘への読込／書戻しが未接続 |
| `skills / spells` | その場での変化を持ち越さず、キャラ定義から再生成 |
| 状態異常・結界・反射 | 戦闘開始時に空配列へリセット |
| 悪夢による勇気減少 | パーティへ保存しない |
| 戦技使用回数 | `combatArtUses={}` へリセット |
| 座標・行動済み・ターン | 戦闘定義／初期状態へリセット |
| 装備変更 | 装備変更システムとその永続化処理はない |
| 敵の状態 | パーティに保存しない |
| 未回収マップアイテム | 戦闘開始時に定義から作り直す |

状態を適用するのは `battleEntrySource === "scenario"` の経路。URL `?battle=...` のテスト、手動・初期化経路は初期能力・初期所持品から開始し、勝利してもパーティへ結果を書き戻さない。ホームのデバッグ起動はパーティを初期化してから戦闘へ入る。

### シナリオとセーブ

- `scenario.js / CHAPTERS` は現在プロローグと第1章を持つ。
- `playCurrentScene` が実行するのは `dialogue / battle`。他の型は次へ進む。探索シーンはコメント上の項目で未実装。
- シナリオ進行は `currentChapter / currentSceneIdx`。勝利後に `advanceScene` する。
- 章終端は `endChapter`。次章へ自動で連結する処理はない。
- ブラウザ `localStorage` の `srpg_save_1〜3` を使用する3スロット。
- 保存可能なのは `currentChapter` があり、`scenarioActive=true` の時だけ。戦闘途中のセーブではない。
- `saveVersion:2`、章ID、シーン番号、背景、パーティ、日時・プレビューを保存。
- 読込時に `createPartyState` で不足データを補完し、シーン履歴から登場人物表示を復元して再開する。
- シナリオのルート分岐・支援関係・クエスト進行等の専用フラグ管理はない。

## 11. UIとゲームロジックの接続

主な参照：`index.html`、`game.js`。ここでは見た目の評価ではなく、表示と処理の接続だけを記録する。

| UI | 主な関数・データ | 接続 |
|---|---|---|
| ステータス詳細 | `openStatusModal → renderStatusModal → renderStatusSheet` | 現戦闘ユニットを優先し、なければキャラ定義を表示 |
| 戦闘予測 | `calculateBattlePrediction → showBattlePreview → renderLandscapeBattlePreview` | 攻撃・対象情報を `_vsAttack` に保持し、確定時に実行 |
| 攻撃等のコマンド | `getLandscapeCommands / renderLandscapeCommandRail / renderLandscapeSubCommandRail` | 行動・移動状態、所持魔法、特技等を条件に表示 |
| マップ | `createGrid / renderUnits / onUnitClick / onCellClick` | 座標とHP、行動済み状態、範囲を描画 |
| 敵危険範囲 | `getEnemyDangerCells / renderEnemyDangerRange` | 敵の現在位置＋到達候補から通常射程の和集合 |
| 敵予告 | `planEnemyActions / renderDeclarations` | 予定移動先と対象IDへの宣言表示 |
| 旧ラジアル・旧予測 | `showAttackRadial / showMagicRadial / showForecastLayer` 等 | コードは残るが現在の戦闘UIは横画面へ分岐。`showForecastLayer` は現行ファイル内に呼び出しがない |

`isLandscapeBattleUi` は画面幅の判定ではなく、現在のゲームモードと `dataset.mode` がともにbattleかを見る。現行の通常戦闘では横画面側が使用される。

### 表示とロジックが一致していない、または表示範囲が異なる箇所

1. **反撃予測は物理反撃のみ。** 実際の `auto / magic_first` の魔法選択を反映しない。
2. **予測の反撃可否が実処理より広い。** 訓練の反撃停止、自発魔法に対して反撃しないこと、攻撃が外れた場合や相手を倒した場合を反映しない。
3. **「戦闘後HP」は確定結果でも期待値でもない。** 通常ダメージを相手から引き、反撃ダメージを自分から引いた仮値。命中・反撃発生・必殺・相手撃破での反撃消失を統合していない。
4. **魔法カウンターの反射を予測しない。** 実処理では両者が結界前ダメージを受けるが、予測は通常の結界後ダメージと勇気反撃を計算する。
5. **範囲魔法のVS予測は「特殊」。** 隕石の複数対象へのダメージやHP変化を予測表へ展開しない。
6. **ステータス画面の「回避最終」は一時効果込みではない。** `bs.evasion` は基礎＋技能分。絶影や忠誠心、攻撃者の殺気による差を含まない。物攻・物防等も忠誠心の位置条件を含まない。
7. **必殺欄は対敵最終確率ではない。** 基礎の必殺値とAPP由来耐性を個別表示。殺気・状態・個別ボーナスは詳細画面の数値に足されない。
8. **修練度表示は `skills`、戦技候補判定は `skillRanks` 優先。** 仮に双方に異なる値があれば表示と利用条件がずれる。
9. **絶影が詳細画面の戦技欄に載らない。** `getAvailableCombatArts(unit)` の既定値が `base="attack"` なので、`base="self"` の絶影は除外。
10. **絶影だけを持っていても特技コマンドが出ない。** 横画面の `getLandscapeCommands` はユーティリティ特技の有無だけで「特技」を表示する。幼アルシェ・Lv4アルシェは回避6以上だが該当ユーティリティ特技がなく、現行の通常操作経路から絶影を選べない。リングホルムは応急手当等があるため到達できる。
11. **一部状態名がオブジェクト文字列になる。** 詳細画面は状態の `name || id || effect` を連結する。多くの状態は `type` しか名称キーを持たないため、`[object Object]` となる。別のユニットパネルには `type` の日本語変換がある。
12. **「素手4」の表示と実判定値が異なる。** 技能候補がない時の攻撃メニューは素手4を表示するが、`executeAttack` では対応キーがなく「攻撃5」へフォールバックする。
13. **武器「未装備」でも威力3で攻撃する。** 装備名表示と暫定武器威力は別経路。
14. **予測の地形「平地」は固定文字列。** 地形能力の計算結果ではない。
15. **危険範囲は敵の通常移動＋通常攻撃範囲。** 魔法射程、スタン、訓練の行動停止を考慮しない。宣言された1行動の範囲とも異なる。
16. **投擲・転移のハイライトと実行条件に差がある。** 詳細は6章・9章。

## 12. 未実装・仮実装・不整合の整理

### 12.1 現在のコードで処理が存在するもの

グリッド移動、直前移動取消、味方／敵フェーズ、物理・投擲・一部魔法、命中・必殺・勇気反撃、魔法反射、結界、状態効果、一部回復・再行動・転移、両断・絶影、殺気・忠誠心、物理AIと予告、敵全滅／味方全滅判定、拾得・回復アイテム、シナリオ戦闘への接続、戦闘間リソース保持、会話中セーブ。

ただし、処理が存在することは「全UIから使える」「説明文の効果を全部満たす」「予測と必ず一致する」を意味しない。

### 12.2 データ定義・保存枠があるが未接続

- 両断・絶影以外の14戦技、殺気・忠誠心以外の12パッシブ。
- `BUILD_MILESTONES / MAGIC_MASTERY`、`buildChoices` を使う選択式成長。
- `skillRanks` の戦闘への読込と戦闘後の更新。
- 戦技の耐久・MP倍率等の宣言的コスト。
- 装備による物防・魔防補正の入力先はあるが、装備マスターや装備操作はない。
- 技能説明にある騎乗、庇う、盗む等の本来の効果。
- 魔法説明の習得条件、習得時間、EDU対抗、一族制限。
- 秘伝・発作は名称を保持・表示するが発動処理がない。
- `luckT / luckTokens` は計算されるが消費・振り直し処理がない。

### 12.3 仮処理・未接続効果

- 召喚、変身、浮遊、分霊、付与：MP消費・成功ログ等のみ。
- 風の吹き飛ばし：ログのみ。
- 補助：状態は付くが判定を変えない。
- 戦闘指揮：命中上昇を表示するが実際には加算しない。
- 集中・挑発・庇う・目星等の特技使用：一般的な成功ログのみ。
- MP不足制限なし、反撃物理射程なし、スタン禁止範囲の不統一、敵予告と現状態の再判定差は現行の処理そのもの。修正は行っていない。

### 12.4 設計資料との相違

以下は設計資料の存在を記録するための比較であり、現行仕様への採用・推奨ではない。

| 資料内の記載 | 現在のコード |
|---|---|
| `BATTLE_RULES_V2.md`：呪文MPコスト固定化 | 威力は固定化されるが、MPは `rollDice` |
| 同資料：敵攻撃はマス対象の予告 | `targetId` を追う予告。対象位置に合わせ実行時移動を再計算 |
| 同資料：反撃は可能な射程・状態で判定 | 物理反撃に射程判定なし、スタン共通抑止なし |
| 同資料：技能1d10判定 | 特技コマンドは技能値×10%の1d100。技能0〜10の成功確率は対応するが、実コードの乱数形式は異なる |
| 同資料：判定イベントマス・運命トークン | 拾得アイテムと運派生値はあるが、イベント判定・トークン消費はない |
| `COMBAT_ARTS_DESIGN.md`：修練節目で補正／戦技等を選択 | 保存枠・別データはあるが選択UIや加算処理はない |
| 同資料：武道・魔導9で1戦闘1回の最終ダメージ2倍奥義 | 武道・魔導の固定加算のみ。2倍奥義は未実装 |
| 同資料後半：属性別戦技ツリーを作らない方針 | 未使用の `MAGIC_MASTERY` と属性戦技定義が残っている |

因果Lv・兵種Lv・クラス成長補正・FE型追撃・双剣2ヒット・デュアル・攻陣防陣・武器相性ブレイク・覚醒モード・数手巻き戻しは、今回の壁打ち等で検討している事項で、現行コードに対応したシステムはない。

### 12.5 古い記述・二重経路・その他の注意点

- `spells.js` 冒頭は転移を「未実装：ログのみ」と説明するが、現在は専用のマス選択→`executeTeleport` が存在する。`executeMagic` の転移分岐だけは説明どおりログのみ。
- `spells.js` の悪夢の効果種別説明には勇気／MP減少があるが、実処理は勇気減少のみ。
- `characters.js` 冒頭の `physicalBonus / magicBonus` はDB／MB式という説明だが、戦闘生成では表示用の「力○」「魔○」を設定する。
- `game.js / calcBonusDice, avgDice, rollCritical` 等は定義が残る一方、現行ファイル内で呼び出しがない。実必殺は `rollContextCritical` 側。
- `dataHelper.js / parseCommandArray` は旧コマンド文字列の変換関数。現在の戦闘開始時に使われない。
- `characters.js` の森の番人MP8は、戦闘開始時にPOW由来12へ上書きされる。ディランMP24もPOW由来17へ上書きされる。生データの `mp/maxMp` と実戦開始値は必ずしも一致しない。
- 予測経路は結界を消費しない。実行経路の `completeDamageContext` は結界を変更した後に `baseAfterBarrier / critAfterBarrier` を再計算するため、これらの補助値は実際に適用した吸収前状態と一致しない場合がある。実HP減算には先に求めた `barrier.damage` を使う。
- `PASSIVE_SKILLS` の `chance / condition / trigger` 全般を自動評価していない。必殺計算が実装されたことだけで `requiresCritSystem` 付きの効果が有効になるわけではない。
- 学習済み／装備済みの和集合で効果が有効になるため、フィールド名だけから一般的な「装備中のみ有効」の挙動を推定できない。

## 13. 今後の確認候補（提案・現行仕様ではない）

再設計時には、先に「戦闘予測と反撃の対応」「MP・状態異常の実処理」「習得／装備／保存の境界」を照合すると、既存処理を引き継ぐ範囲を判断しやすい。この文書では変更案や新しい数値仕様は確定しない。

## 付録：主要参照先

行番号は調査時点。後の編集で変わるため、関数・データ名を併記する。

| ファイル | 主な位置・参照対象 |
|---|---|
| `characters.js` | `CHARACTERS_DATA`（13行付近）。キャラ9体 |
| `statConversion.js` | `statAt`（6）、`calcBattleStats`（42）、命中・回避（101〜）、必殺（144〜） |
| `dataHelper.js` | `fixedDiceValue`（102）、`getWeaponPower`（122）、`getSpellPower`（128） |
| `partyState.js` | `createPartyState`（22）、`getPartyBattleResources`（55）、`updatePartyStateFromBattle`（84） |
| `battleDefinitions.js` | `BATTLE_DEFINITIONS`（5） |
| `battleHooks.js` | `createBattleActionContext`（19）、`runBattleActionHooks`（87） |
| `combatArts.js` | `COMBAT_ARTS`（17） |
| `passiveSkills.js` | `PASSIVE_SKILLS`（13） |
| `buildMilestones.js` | `BUILD_MILESTONES`（21）、`MAGIC_MASTERY`（102）。未読込 |
| `spells.js` | `SPELLS_DATA`。現行魔法の実行用定義 |
| `skills.js / magics.js` | `SKILLS / MAGICS`。説明・原作寄りの定義 |
| `game.js` | 移動（867〜）、命中・ダメージ（979〜）、戦技・フック（1200〜）、横画面コマンド（1633〜） |
| `game.js` | 反撃（2017〜）、物理（2135〜）、転移（2294〜）、魔法（2378〜）、特技（2693〜） |
| `game.js` | ターン（2724〜）、予測（2795〜）、敵宣言（3750〜）、AI（3927〜）、勝敗（4161〜） |
| `game.js` | ステータス詳細（4494〜）、シナリオ接続（5029〜）、戦闘生成（5143〜）、セーブ（5327〜） |
| `scenario.js` | `CHAPTERS`。会話・戦闘の順序 |
| `index.html` | DOM要素とスクリプト読込。`buildMilestones.js` は含まない |
| `map_editor.html` | `updateOutput / copyOutput`。マップ定義テキストの作成補助 |
| `BATTLE_RULES_V2.md / COMBAT_ARTS_DESIGN.md` | 実装とは区別して相違点を確認した設計資料 |

画像・音声などの素材内容や、TRPG原作資料の再解釈は本調査の対象外。ゲームコードの変更、リファクタリング、新機能実装は行っていない。

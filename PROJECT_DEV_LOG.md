# Project Dev Log

このファイルは、自作SRPGプロジェクトの開発日誌です。
Codex / Claude Code が途中から参加しても、前回までの成果、残課題、次に優先すべきことをすぐ把握できるように残します。

## 書き方

各日の最後に、以下の形で追記します。

```text
## YYYY-MM-DD

### 今日の成果
- 

### 残っている課題
- 

### 次回の優先候補
- 

### メモ
- 
```

## 2026-09-21

### 今日の成果

- PCでステータス画面が旧デザインへ戻り、上下が切れる問題を修正した
  - 原因は詳細シートの表示条件が「1200px以下の横画面戦闘」に限定されていたことと、旧640px高モーダルが390px高のゲーム領域内に置かれていたこと
  - 立ち絵付き詳細シートをPC・スマホ縦横で共通化し、モーダルをゲーム本体の拡大縮小・クリッピングから独立させた
  - 当初のサンセリフ・ニュートラル色への変更は原作者の意図と異なったため撤回し、元の黒紫・金・明朝体・紋章の意匠へ戻した
  - 横画面は元の828×374シートの比率を保って画面に合わせて拡縮する。PCの幅1200px超でも同じシートを表示する
  - 原作者から横画面専用との指定を受け、追加した縦積み表示は撤去。ホーム・会話・戦闘・ステータスすべてで横長比率を維持し、縦長ウィンドウでは全体を縮小して収める
  - Escで閉じる操作、フォーカス復帰、背景の操作抑止を追加した
- 確認用ローカルサーバーを `http://127.0.0.1:8000/` で起動した
- 原作者が変更前の横画面シートを今後のUI基準として指定。`PROJECT_CONSTITUTION.md` と `CLAUDE.md` に明記した

### 検証

- `node --check game.js`
- `node tests/battleHooks.test.js` / `node tests/statConversion.test.js` / `node tests/partyState.test.js` 成功
- ブラウザでPC 1280×720、スマホ横852×393、縦390×844を確認
- 見切れ・横方向のオーバーフローなし、縦画面で末尾の魔法までスクロール可能、Escで閉じて元の操作へ戻ることを確認
- ブラウザのエラー・警告なしを確認

### 残っている課題

- ユーザーによる見た目の確認待ち。今回の変更は未コミット・未push
- シナリオ立ち絵先読み、戦闘効果基盤、装備正式化は今回の対象外として継続保留
- 以前の作業キューには絶影・殺気・忠誠心の最小実装前の記述が残る。再開時は既存実装を前提に整理する

## 2026-07-17

### 今日の成果

- 新しい立ち絵透過ワークフローを本体運用へ接続した
  - `tools/tachie_studio.py` と一括下処理ツール、依存関係、運用ドキュメントをリポジトリへ追加した
  - 仕上げ済みのギュンター、幼少期アルシェ表情差分、幼少期カリマをゲーム参照へ反映した
  - 画像URLへ素材版番号を付け、同名PNG更新時にSafari等の旧キャッシュが残る問題を防いだ
- シナリオ画面の人物表現を改善した
  - 話者だけが軽く呼吸・揺れるCSS演出を追加し、APNG/WebMを常時再生せずに「生きている感」を出した
  - アルシェとカリマを上半身中心の会話向け縮尺へ調整した
  - 「呆れ」アルシェは右手まで見える専用フレーミングを設定し、古いデバッグ保存値も一度だけ新版へ移行するようにした
  - 立ち絵を人数分の固定区画から独立レイヤーへ変更した
  - 髪や手が隣のキャラ領域へ重なっても切れず、話者が自動的に前面へ切り替わる構造にした
  - 1〜4人用の基本配置を用意した
- ホーム画面を横画面用イラストへ差し替え、横長画面で人物が切れにくい表示位置へ調整した
- 立ち絵ツール・透過素材・本体表示変更を `783c8fe` としてコミットした

### 検証

- `node --check characters.js / scenario.js / game.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`
- `py -3.12 -m py_compile` で立ち絵ツール3本を検証
- iPhone 16e横画面相当の852×393で、3人表示、話者の前後切り替え、右手の重なり、縦横オーバーフローなしを確認
- ブラウザのエラー・警告なしを確認

### 残っている課題

- 表情差分を初めて表示する瞬間、未キャッシュ画像の読み込みで立ち絵が一瞬遅れて出る可能性がある
- アルシェの全表情差分と、1人・2人・4人配置は実画面での最終確認がまだ必要
- `立ち絵透過下処理/` の残り素材は、人手で輪郭・白い服・閉じた隙間を確認してから正式素材へ移す
- `battle-v2` のローカルコミット群は、まだ `origin/battle-v2` へpushしていない
- 必殺カットインの稲妻は演出面の宿題。現行案を磨くより、帯そのものを専用演出として再設計した方がよい
- 作業ツリーには既存の未追跡素材・メモが残っている。用途を確認せず一括追加・削除しない

### 次回の優先候補

1. `battle-v2` をpushし、GitHub Pagesまたはスマホ実機で透過立ち絵とレイヤー切り替えを確認する
2. シナリオ立ち絵の先読みを追加し、初回表示の一瞬の空白を解消する
3. アルシェ全表情と1〜4人配置を短時間で点検し、立ち絵UIを一旦完成扱いにする
4. `BattleActionContext`と効果フックへ戻り、両断に続いて絶影・殺気・忠誠心を共通基盤で扱えるようにする
5. 装備・武器威力・射程を正式データ化し、戦闘予測の「未装備」を実データへ置き換える

### メモ

- 固定区画へ立ち絵を押し込む方式は、ポーズ差分が増えるほど手足の見切れが起きる。独立レイヤー方式を今後の標準とする
- 立ち絵UIは完成に近づいた。次回は確認作業を短く区切り、ゲームの中心である戦技・パッシブ実装へ戻る
- 未追跡ファイルは今回のコミットへ混ぜていない

## 2026-07-16

### 今日の成果

- 立ち絵の背景透過ワークフローを更新した
  - `tools/tachie_studio.py` を導入し、自動透過からブラウザでの手直しまでを1画面にまとめた
  - `isnet-anime` をONNX Runtimeから直接実行し、WindowsではDirectML対応GPUを自動利用するようにした
  - `rembg`の重い起動処理とCPU推論を避け、1枚数分だった処理を起動込み約6秒まで短縮した
  - 未処理素材だけを抽出する `tools/prepare_tachie_batch.py` を追加した
  - 白背景の立ち絵16枚を `立ち絵透過下処理/` に生成した。元画像とゲーム参照は変更していない
- 戦闘中の細かい操作感を改善した
  - 移動後、行動確定前なら「戻る」で移動前のマスへ戻せるようにした
  - 移動先で拾ったアイテムも、移動キャンセル時に所持品から外してマップへ戻すようにした
  - 待機後に右側コマンドが残る問題を修正した
- 魔法攻撃・魔法反撃が外れた時にも `Miss` ポップアップが出るようにした
- 必殺カットイン用のポートレート調整機能を追加した
  - 既存の5タップ立ち絵調整パネルに「必殺」タブを追加
  - キャラごとに必殺カットインの拡大率、X位置、Y位置を保存できるようにした
  - 調整中は必殺カットインのプレビューを表示するようにした
- 必殺カットイン演出を試作した
  - 赤金系の稲妻アクセントを追加
  - キャラ絵の輪郭に電光フラッシュを追加
  - ただし、稲妻が必殺帯と自然に馴染みきっていないため完成扱いにはしない
- シナリオ会話中の立ち絵に、静止画の「生きている感」を補う呼吸演出を追加した
  - APNG/WebMやCanvasの常時描画は使わず、話者だけをCSS `transform`で動かす軽量構成にした
  - 呼吸の伸縮と長周期の微かな揺れを別レイヤーにし、既存の立ち絵トリミング調整と競合しないようにした
  - 会話送りで立ち絵DOMが再生成されても動きが跳ねないよう、実時間をアニメーション位相へ反映した
  - `prefers-reduced-motion` では停止する
- ホーム画面の背景を横画面用の `立ち絵AI生成/ホーム画面横向き(仮).png` へ差し替えた
  - 横長ゲーム画面で人物の頭頂が切れないよう、背景の表示基準を上寄りへ調整した
- 仕上げ済みの透過立ち絵をゲームへ反映した
  - アルシェの幼少期表情差分11枚、幼少期カリマ、ギュンターを既存の正式ファイル名へ統一した
  - `scenario.js` の既存表情指定と保存済みトリミング設定を維持したまま素材内容を更新した
  - `characters.js` のアルシェ、カリマ、ギュンターの基本ポートレートを `立ち絵透過済み/` 参照へ変更した
  - 透過版が未完成のアルシェ被弾差分は従来素材のまま維持した
  - 画像URLへ素材版番号を付け、同名PNGを更新した際にブラウザの旧キャッシュが残らないようにした
  - 会話画面のアルシェとカリマを上半身中心の縮尺へ揃えた
  - 「呆れ」表情は右手が切れない専用フレーミングと、旧デバッグ保存値の一度限りの移行を追加した

### 検証

- `node --check game.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`
- iPhone 16e横画面相当（852×393）でシナリオ画面を確認し、縦横のオーバーフローが増えないことを確認
- 同画面サイズでホーム画像、タイトル、メニュー、デバッグ欄が画面内に収まることを確認
- シナリオでギュンター、アルシェ、カリマの3人を同時表示し、透過素材の読み込みと表示を確認

### 残っている課題

- `立ち絵透過下処理/` の16枚は人手で輪郭・白い衣装・閉じた隙間を確認し、仕上げ後に `立ち絵透過済み/` へ移す
- 完成素材へのゲーム参照差し替えは、仕上げ確認後に別作業として行う
- 必殺演出は要再設計
  - 現状は「稲妻を帯に足した」段階で、必殺帯の縁取りとして自然に見える完成形ではない
  - 次回は帯そのものを作り替えるか、専用Canvas/SVGレイヤーで電撃を描くかを検討する
  - 参照イメージは「縁取りに稲妻が走る」「帯と一体化している」「別パーツに浮いて見えない」方向
- 必殺カットイン調整パネルは便利だが、デバッグ専用であることをUI上でも分かりやすくしてよい
- 今日のコミット群はまだ `origin/battle-v2` へ push していない

### 次回の優先候補

- まず必殺演出を深追いしすぎず、戦闘UXの小さな違和感をもう少し潰す
- 必殺演出を再挑戦する場合は、現行の帯に足すのではなく、専用演出として設計し直す
- `battle-v2` の未pushコミットを必要なタイミングでまとめてpushする

### メモ

- 今日の必殺演出試作は失敗ではなく、方向性の確認として有用
- 「切れないように外へ出す」と浮き、「帯の中へ入れる」と縁取り感が弱い。次はレイヤー構造そのものを見直す
- 操作感の改善はプレイ体験に直結するので、今後も大改修の合間に優先して拾う

## 2026-06-23

### 今日の成果

- GitHub Pages に最新版を反映し、スマホ Safari から確認できるようにした
- スマホ向けUIを調整した
  - Safari 下部バー対策として、スマホ時の画面スケールに安全余白を追加
  - 上部タブを少し押しやすく調整
  - ラジアルメニューのボタンを少し大きく調整
  - 下パネルの「特技 / 魔法 / 詳細」ボタンを押しやすく調整
- 中央マップエリアの横幅を上下パネルと揃え、凹凸感を軽減した
- マップ右下に `視点` ボタンを追加した
  - `視点` で斜め見下ろし風
  - `2D` で通常の俯瞰表示へ戻る
- `PROJECT_CONSTITUTION.md` を作成した
  - このゲームの最上位方針
  - スマホ操作、SRPG快適性、TRPGらしさ、世界観、品質基準を明文化
- `TRPG_SRPG_DESIGN_THINKING.md` を作成した
  - 反証エージェント
  - 発想・レビュー観点
  - Codex / Claude に投げるための設計テンプレ
- 上記2ファイルをコミットし、GitHub へ push 済み

### 残っている課題

- スマホ戦闘画面がまだ小さく、操作しづらい可能性がある
- 斜め見下ろし風モードは初版なので、見やすさとタップしやすさの検証が必要
- 縦画面だけで快適にするか、横画面対応も検討するか判断が必要
- ラジアルメニュー、下パネル、マップ表示の情報密度をさらに整理したい
- SRPG として「遊んでいて快適」な操作導線をもう一段詰めたい

### 次回の優先候補

- スマホ実機で `視点` ボタンを触り、2D / 斜め表示の操作感を確認する
- `PROJECT_CONSTITUTION.md` と `TRPG_SRPG_DESIGN_THINKING.md` を前提に、スマホ戦闘UIを反証・発想レビューする
- マップ表示改善案を 3 から 5 個出して比較する
  - 縦画面2D改善
  - 疑似3D強化
  - 横画面対応
  - 戦闘時だけマップ拡大
  - 下パネル折りたたみ
- 一番リスクが低く効果が高い案を小さく実装する

### メモ

- 原作者の重視点は「ビジュアルの良さと遊びやすさの両立」「スマホで遊べる操作感」「SRPGとしての快適さ」「TRPGらしさ」「世界観の調和」
- 迷ったら `PROJECT_CONSTITUTION.md` を最優先で参照する
- 大きな設計判断では、Codex / Claude は 3 から 5 案を出して比較する
- 完全放置で常駐作業はできないため、依頼時は目的・制限・止まる条件を明確にすると自走しやすい

## 2026-07-15

### 今日の成果

- TRPG原値を使うFE型戦闘計算へ移行した
  - 物理: `力 + 武器威力 - 物防`
  - 魔法: `魔力 + 魔法威力 - 魔防`
  - 武道・魔導は修練度3/6/9で最終ダメージ`+1/+2/+3`
  - 防御計算前の攻撃値を確率で2倍にする旧武道処理を削除
- 必殺システムを実装した
  - `勇気/2 + レベル補正 - 魅力*2`
  - 最終ダメージ3倍、敵・魔法・反撃・範囲攻撃にも対応
  - 戦闘予測とステータス画面へ必殺率・必殺回避を追加
  - 「悪夢」の勇気低下が反撃率と必殺値の両方へ影響するよう変更
- 攻撃実行が無反応に見える問題を対策した
  - 戦闘処理完了後に予測画面を閉じる順序へ変更
  - 例外を戦闘ログとSYSTEMメッセージへ表示
  - `game.js` / `statConversion.js` / `style.css` のキャッシュ識別子を更新
- 必殺カットインを実装した
  - 攻撃者の立ち絵、キャラ名、「必殺 / CRITICAL STRIKE」を横断帯で表示
  - 味方と敵で進入方向を反転
  - 物理・魔法・反撃で共通利用
  - カットイン終盤から必殺ダメージ数字を表示
- 実戦ログで新計算を確認した
  - リングホルム→森の番人: 通常16、必殺48
  - ディラン→リングホルム: 通常21
  - 旧武道による56ダメージは最新版では再現しない

### 残っている課題

- 修練度9で解放する1戦闘1回の2倍奥義は未実装
- 2倍奥義・必殺・範囲攻撃など倍率効果の排他制御が未実装
- 戦技・パッシブを安全に差し込む共通効果フックが未実装
- 武器・装備・耐久・使用回数が仮データのまま
- 敵AIは戦技・魔法・必殺期待値を十分に評価できていない
- 敵側カットインとキャラ別トリミングをスマホ実機で継続確認する
- 必殺カットインのSE、画面振動、ヒット演出は未調整

### 次回の優先候補

1. 戦闘行動コンテキストと効果フックを設計する
2. 2倍奥義の使用回数・排他ルールを実装する
3. 装備武器と武器威力を正式データ化する
4. 必殺率と3倍ダメージを複数キャラ対戦で再検証する
5. 敵AIの魔法・戦技評価へ進む

### メモ

- 基礎戦闘式は概ね揃い、次は「戦技を増やしても計算順が壊れない器」が最優先
- 最新ログでは旧武道処理が消えていることを確認済み
- 必殺カットインはiPhone横画面相当で、顔・上半身・文字が収まることを確認済み

## 2026-07-15 追加

### 今日の成果

- `battleHooks.js` を追加し、`BattleActionContext` と `beforeAttack / beforeDamaged / afterDamage / afterAttack / onKill` の共通フックを定義した
- Context に命中、必殺、基礎ダメージ、最終ダメージ、実ダメージ、対象一覧、使用回数、発動済み効果を保持できるようにした
- 物理攻撃、魔法ダメージ、破壊魔法、範囲魔法、物理反撃、魔法反撃のダメージ確定を Context 経由に寄せた
- 戦闘予測と実ダメージが同じ Context 生成関数を通るようにし、予測表示では命中乱数を消費しないようにした
- 範囲魔法では同一 Context を使い回せるようにし、将来の抽選パッシブが1行動内で重複発動しない土台を作った
- 最初の検証対象として「両断」を `beforeAttack` フックで実装し、物理攻撃の攻撃値+5として動くようにした
- 横画面/通常攻撃メニューから、条件を満たすユニットが「両断」を選べる入口を追加した
- `tests/battleHooks.test.js` を追加し、`oncePerAction` フックが1行動で重複発動しないことを確認した

### 残っている課題

- 反撃手段の自動選択ロジックはまだ期待値比較に旧 `calculatePhysicalDamage` / `calculateMagicDamage` を直接使っているため、次回 Context 経由へ寄せる
- 両断以外の `絶影 / 殺気 / 忠誠心` は未実装。Context が安定したら1つずつ追加して検証する
- 戦技コストの武器耐久、使用回数、装備枠、習得/装備UIは未接続
- `beforeDamaged` 系パッシブの確率抽選、効果時間、重複ルールはまだ実装前
## 2026-07-15 追加2

### 今日の成果

- `partyState.js` に戦技・パッシブ・ビルド選択の永続保存枠を追加した
  - `learnedArts`
  - `equippedArts`
  - `learnedPassives`
  - `equippedPassives`
  - `buildChoices`
- 戦闘開始時に、partyState 側の戦技・パッシブ情報を live unit へ渡すようにした
- `絶影` を最小実装した
  - `IMPLEMENTED_COMBAT_ART_IDS` に `zetsuei` を追加
  - `beforeAttack` フックで `evasionUp` 状態を付与
  - 効果は「発動ターン中、回避+20」
  - `cost.uses` を見て戦闘中3回までの使用回数を管理
- `evasionUp` を命中率計算へ反映した
  - 戦闘予測、実戦闘ともに `getBattleHitResult` 経由で同じ補正を読む
- 横画面/旧UIの特技コマンドに `base: "self"` の戦技を出せる入口を追加した

### 検証

- `node --check game.js`
- `node --check partyState.js`
- `node tests/partyState.test.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`

### 残課題

- `絶影` のログ文言は安全優先で仮。後で世界観に合う表記へ整える
- `絶影` の使用回数をUI上で `残り2/3` のように見せる表示は未実装
- `殺気`、`忠誠心` は未実装。次は1つずつフックに載せる
- 旧UI/横画面UIで特技表示ロジックがまだ重複しているため、後で共通化する
## 2026-07-15 追加3

### 今日の成果

- Excelの `戦技案.xlsx` を読み取り、`殺気` と `忠誠心` の原案を確認した
- `殺気` を最小実装した
  - `passiveSkills.js` に `sakki` を追加
  - リングホルムの `learnedPassives / equippedPassives` に `sakki` を追加
  - 本体の `IMPLEMENTED_PASSIVE_SKILL_IDS` に `sakki` を追加
  - 能動攻撃時のみ、命中+10・対象回避-10・必殺+10を反映
  - 反撃時には発動しないよう `isCounter` を反撃命中計算へ渡した

### 検証

- `node --check game.js`
- `node --check characters.js`
- `node --check passiveSkills.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`

### 残課題

- `殺気` の「勇気差で相手の反撃不可」は未実装
- `忠誠心` は未実装。2マス以内に味方がいる場合の派生戦闘値+3として実装予定
- パッシブの命中補正は `getBattleHitResult` に寄せたが、今後 `BattleActionContext` 側へさらに集約する余地がある
## 2026-07-15 追加8

### 今日の成果

- 旧topLayerの戦闘予測カードにも、BattleActionContext由来の補正タグを表示するようにした
- 物理攻撃、反撃、魔法攻撃の予測カードで「両断」「殺気」「忠誠心」などの発動予定を確認できる
- 横画面UIと旧予測UIのどちらでも、補正の理由を追跡できるようになった

### 検証

- `node --check game.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`

### 残課題

- 予測タグの見た目は暫定。最終UIでは戦技/パッシブ専用の小さな紋章タグに寄せる
- 発動効果が増えたら、表示順と省略ルールを決める

## 2026-07-15 追加7

### 今日の成果

- BattleActionContext の内部メモを、ログ/UI表示時に日本語ラベルへ変換する `formatActionEffectNoteLabel()` を追加した
- `ryoudan:power+5` などの内部IDを、表示時は「両断:攻撃+5」のように読める形へ整えた
- 両断の内部メモも他の効果と同じID形式へ統一した

### 検証

- `node --check game.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`

### 残課題

- 効果メモの表示名は今後増える戦技/パッシブに合わせてテーブル化する
- UI表示では最終的に「補正」欄として専用デザインへ寄せる

## 2026-07-15 追加6

### 今日の成果

- 戦闘予測データに `effectNotes / counterNotes` を返すようにした
- 横画面戦闘予測UIに、発動予定の戦技/パッシブ補正を1行タグとして表示するようにした
- 予測と実ログが同じ `BattleActionContext.notes` を参照する形になり、補正の追跡がしやすくなった

### 検証

- `node --check game.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`

### 残課題

- 旧topLayer予測パネルにも同じ補正タグを出すか検討する
- 表示名は内部ID寄りなので、UI文言として「両断 攻撃+5」「殺気 必殺+10」のように整える

## 2026-07-15 追加5

### 今日の成果

- BattleActionContext の `notes` を戦闘ログへ表示するための `formatActionContextNotes()` を追加した
- 通常攻撃、反撃、魔法、破壊、範囲魔法、自己戦技のログに発動メモを差し込んだ
- 「両断」「絶影」「殺気」「忠誠心」の発動や補正が、実戦闘ログ上でも追跡しやすくなった
- `忠誠心` は発動条件を満たした時に `chuuseishin:attacker+3` / `chuuseishin:defender+3` としてContextへメモを残すようにした

### 検証

- `node --check game.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`

### 残課題

- ログ文言はまだ内部ID寄りなので、最終的には世界観に合わせた日本語表示へ整える
- 戦闘予測パネルにも発動予定の戦技/パッシブ名を出すか検討する

## 2026-07-15 追加4

### 今日の成果

- パッシブ「忠誠心」を最小実装した
  - `passiveSkills.js` に `chuuseishin` を追加
  - ギュンターへ `learnedPassives / equippedPassives` として付与
  - 2マス以内に同陣営の生存ユニットがいる場合、戦闘中の派生戦闘値へ+3を加える
- 補正対象は現時点では以下に限定した
  - 物理攻撃値
  - 魔法攻撃値
  - 物防
  - 魔防
  - 命中計算の補正
  - 回避計算の補正
- 生のTRPG原値は書き換えず、戦闘計算時だけ `getDerivedPassiveStatBonus()` で足す形にした

### 検証

- `node --check game.js`
- `node --check characters.js`
- `node --check passiveSkills.js`
- `node tests/battleHooks.test.js`
- `node tests/statConversion.test.js`
- `node tests/partyState.test.js`

### 残課題

- 「忠誠心」の+3を命中/回避へそのまま足すか、DEX/STR相当として変換するかは実戦テストで再確認する
- 位置依存パッシブの単体テストは `game.js` から戦闘計算を切り出してから追加する
- 次の候補は「絶影」または「忠誠心」のUI表示/発動ログ整理

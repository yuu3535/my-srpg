# 戦闘ルールv2 設計メモ

2026-07-13時点のClaude/Codex共通参照用メモ。
TRPG由来のキャラクター資産をSRPG戦闘へ接続するため、既存エンジンのうちグリッド、BFS移動、描画、マップエディタ、シナリオ接続は維持し、戦闘ルール層を再設計する。
宣言制パッチ適用後を前提にする。

## 0. 設計思想

TRPGの快感とSRPGの快感を同じ乱数に押し込めない。

- 戦闘レイヤーは確定寄りにする。ダメージ、回数、状態は読める値にし、宣言制と組み合わせて位置取りの読みを報わせる。
- 判定レイヤーはダイスを残す。技能判定、判定イベントマス、武道発動、秘伝、運命トークンなどに乱数を置く。
- 失敗しても詰まない。展開や報酬が変わるだけにする。

## 1. TRPG原値を正典にした戦闘ステータス

内部データはTRPGシートの生値を正典にする。
STR・DEX・POWは縮小せずSRPG上の力・技量・魔力として使い、物防と魔防だけ複数能力から算出する。

### 1-1. characters.jsの形式

TRPGシートの `26(16+3.3.2.2)` 表記を構造化する。

```js
// 旧
str: 26,

// 新
str: { base: 16, gains: [3, 3, 2, 2] },
```

現在値の解決は `dataHelper.js` に集約する。

```js
function statAt(stat, level) {
    if (typeof stat === "number") return stat;
    return stat.base + stat.gains.slice(0, level - 1).reduce((a, b) => a + b, 0);
}
```

### 1-2. 正典データ

| 能力 | リングホルム | アルバス | アルシェ |
|---|---:|---:|---:|
| HP | 12 +[2,3,3,3] | 18 +[2,2,3,3] | 13 +[3,3,3] |
| STR | 16 +[3,3,2,2] | 12 +[1,2,2,1] | 14 +[3,3,2] |
| CON | 13 +[3,1,3,3] | 21 +[3,1,1,3] | 13 +[2,3,2] |
| DEX | 15 +[3,2,1,1] | 24 +[3,3,3,3] | 18 +[1,3,1] |
| POW | 16 +[3,3,3,3] | 24 +[3,3,3,3] | 18 +[3,3,2] |
| EDU | 14 +[2,3,2,3] | 18 +[2,3,3,2] | 16 +[3,3,2] |
| INT | 14 +[3,3,3,2] | 16 +[2,2,3,3] | 14 +[2,2,2] |
| SIZ | 10 | 15 | 13 |
| 勇気 | 90 | 65 | 90 |
| 幸運 | 35 | 65 | 90 |

アルシェのLv5成長値は未決。物語のクライマックス演出と同期して決める。

### 1-3. TRPGステータスから戦闘ステータスへの変換

| 戦闘ステータス | 変換式 | 役割 |
|---|---|---|
| 力 | `STR` | 物理攻撃値 |
| 魔力 | `POW` | 魔法攻撃値、MP |
| 技量 | `DEX` | 命中・回避の基礎 |
| 物防 | `floor((STR + SIZ) / 2)` | 物理軽減。腕力と体格による防護 |
| 魔防 | `floor((POW + CON) / 2)` | 魔法軽減。魔力と生命力による抵抗 |
| 反撃率 | `courage%` | 被攻撃後の反撃発生率 |
| 運 | `floor(luck / 10)` | 運命トークン初期数 |
| HP | シート値そのまま | 耐久 |
| MP | POW現在値そのまま | 弾数資源 |

INT・EDUは将来の器量ポイント、技能値は修練度として育成・戦技習得へ接続する。
回復などの支援効果は攻撃値と分離し、暫定的に `floor(POW / 3)` を効果量へ使う。

検算基準:

- リングホルムLv5: 力26、魔力28、技量22、物防18、魔防25
- アルバスLv5: 力18、魔力36、技量36、物防16、魔防32
- アルシェLv1: 力14、魔力18、技量18、物防13、魔防15
- アルシェLv4: 力22、魔力26、技量23、物防17、魔防23

リングホルムとアルバスは平均値ではなく、レベル・役割とも完成した天井ユニットとして検算する。
序盤主人公と一般兵を別の基準にし、天井ユニットが強いこと自体は抑制しない。

実装は `statConversion.js` に集約する。新キーは `atk/mag/skl/def/res/counterRate/luckT`。
移行中のUI互換用として `power/magic/technique/armor/ward/luckTokens` も同値で返す。
検算は `node tests/statConversion.test.js` で、`characters.js` の実データを直接確認する。

APPはTRPGシート値を正典として復元し、現時点では注目度として使用する。
敵AIの評価値は `距離×10 - APP - 注目補正`。距離を優先し、同程度ならAPPが高い対象へ向かう。

## 2. 確定ダメージ式

### 2-1. 物理

```text
物理ダメージ = STR + 武器・戦技威力 - 相手の物防
最低1
```

武器威力はダイス表記を固定化する。

- 1d4 -> 2
- 1d6 -> 3
- 1d8 -> 4
- 1d10 -> 5

同格ユニット間の1発は相手最大HPの30から40%程度を基準にする。
武道は判定レイヤーに残し、`1d10 <= 武道値` 成功で `力 + 武器威力` を2倍にする。

### 2-2. 魔法

```text
魔法ダメージ = POW + 呪文威力 - 相手の魔防
最低1
```

呪文の `powerFormula` も平均固定化する。回復と結界も同じ方針で確定値へ寄せる。

### 2-3. 命中

命中はA/Bを比較できるようにする。

- 案A: 現行の対抗ロールを残し、命中率を20から95%にクランプする。
- 案B: 命中は100%。回避を回避カウント制にして、任意発動の確定資源にする。

推奨は案Bの試作。ただしTRPGの手触りが消えすぎる場合は案Aへ戻す。

### 2-3a. 物理命中・回避の試作式

2026-07-14から次の式を実プレイ検証する。

```text
攻撃精度 = DEX × 2 + 使用技能 × 5 + 命中補正
回避補正 = (DEX - SIZ) + 回避技能 × 3 + 地形補正
最終命中率 = clamp(攻撃精度 - 回避補正, 20, 95)
```

回避技能は全攻撃へ常時作用するため、攻撃技能より係数を抑える。
戦闘予測、実攻撃、敵AI、物理反撃は同じ計算関数を使用する。

攻撃・妨害魔法も使用者のDEXと魔法成功値を同式へ入れる。
回復・結界・加速など味方対象の補助魔法は100%成功とする。
攻撃魔法に付随する状態異常は、命中後に追加抽選せず、ダメージなどの固有条件を満たせば付与する。

### 2-4. 反撃

現行の期待値自動選択システムは資産として維持する。
反撃可能な射程・状態なら、`courage%` で反撃が発生する。

## 3. MP経済

- MPはPOW現在値。
- 呪文コストも固定化する。
- 章内持ち越し制は推奨。ただしパーティ永続層の実装後に行う。

## 4. 勇気/反撃システム

HPと並ぶメンタルゲージとして扱う。

- 反撃率 = `courage%`。
- 反撃可能な射程・状態でのみ判定する。
- 勇気0では反撃不能。
- 操作不能は少人数編成ではストレスが強いため採用しない。
- 回復手段は最初からセットで実装する。

## 5. 判定レイヤー

- 技能判定は `1d10 <= 技能値`。
- 判定イベントマスは `mapItems` の拡張で扱う。
- 運命トークンは判定の振り直し資源。初期数は運。
- clan限定技能は敵アーキタイプの素材として使える。

## 6. 宣言制との統合

- 敵の攻撃はマス対象として予告する。
- 確定ダメージ化により、予告UIにダメージ量まで出せる。
- `calculateBattlePrediction` は期待値計算が不要になり、確定値表示へ簡素化できる。

### 6-1. テスト戦闘と本番戦闘の共通化

- URLの `?battle=<battleId>` とシナリオの `battleId` は、どちらも `startBattleSession()` を経由して同じ戦闘エンジンを起動する。
- テスト専用の戦闘ロジックは作らない。テストと本番の差は開始元、戦闘定義、戦闘後の復帰先だけに限定する。
- 背景、盤面、参加ユニット、初期配置、勝敗条件は `battleDefinitions.js` に置く。
- 戦闘定義は `tests/battleDefinitions.test.js` でキャラID、盤面範囲、配置重複を検査する。
- シナリオ開始時は `source: "scenario"`、URL直行は `source: "test"` として記録する。シナリオ経由の勝利時だけ次の会話へ戻る。

## 7. Codex実装順序

今回の第一バッチは1から3までに限定する。

1. `dataHelper.js` に `statAt()` と `calcBattleStats(unit)` を追加する。旧形式の数値ステータスとの後方互換を維持する。
2. `characters.js` の主要3人を `base + gains` 形式へ移行する。
3. `resolvePhysicalHit`、魔法解決、反撃を確定式へ差し替える。武道判定のみ乱数を残す。命中A/Bはフラグ切り替え可能にする。
4. ステータス画面と戦闘予測を変換後ステータス表示へ変更する。
5. 勇気を反撃率として予測UIと実行処理へ接続する。
6. MPコストを固定化する。章持ち越しはパーティ永続層の後。

## 8. 未決事項

- アルシェのLv5成長値。
- 敵ユニットの `base + gains` 化。
- 呪文ごとの威力、コスト固定値一覧。
- 命中A/B案の実プレイ判定。
- 恐慌時のペナルティ数値。
- 敵側に純物理型を必ず用意する。力が高く魔防が低い敵がいないと、物防と魔防の相性関係が立ち上がりにくい。

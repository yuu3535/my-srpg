# CLAUDE.md — 自作SRPGプロジェクト

このファイルはClaude Codeへのプロジェクト指示書です。作業前に必ず目を通すこと。

## プロジェクト概要

JavaScript / HTML / CSS製の自作シミュレーションRPG(ビルドツールなし・ブラウザ直接実行)。
ダークファンタジー世界観。主人公はアルケー(13歳の少年・百年戦争編)。
GitHub Pagesで公開中: https://yuu3535.github.io/my-srpg/

- UIカラーテーマ: ヴァイオレット × ゴールド × アンバー
- 画面は横画面専用（2026-09-21 原作者指定）。縦長ウィンドウでも横長比率を維持し、ホーム・会話・戦闘・ステータスを縦配置へ切り替えない。
- UI意匠は `1efae8f` の横画面ステータスシートを基準にする（2026-09-21 原作者確認）。黒紫の下地・紫の帯・金の細罫線・明朝体・控えめな紋章を維持し、可読性改善を理由に汎用ダッシュボード風へ変更しない。詳細は `PROJECT_CONSTITUTION.md` の「UIの意匠基準」を参照。
- エントリポイント: `index.html`
- 主要スクリプト: `game.js`(本体)、`characters.js`、`skills.js`、`magics.js`、`spells.js`、`scenario.js`、`dataHelper.js`
- スタイル: `style.css`
- 素材: `assets/`、`Character/`、`背景/`
- 設定資料: `CharaStatus/`、`シナリオ集/`、`SRPG戦闘システム_ClaudeCode用仕様整理.pdf`

## 立ち絵の背景透過ワークフロー(重要・確立済み)

立ち絵はAI生成(白背景)→ 透過処理 → ゲームで使用、という流れ。
透過処理には原則 `tools/tachie_studio.py` を使うこと。**自前で透過処理を再実装しない。**
旧 `tachie_clear.py` は過去素材の再現・比較用として残しているが、新規素材には使わない。

### フォルダの役割
- `立ち絵AI生成/` … AI生成した白背景の立ち絵を入れる場所(入力)
- `立ち絵透過下処理/` … AIで自動透過した、人手確認前のPNGを置く場所
- `立ち絵透過済み/` … 人手で仕上げと確認を終えた、ゲーム採用可能なPNGを置く場所

### 初回セットアップと起動

```powershell
py -3.12 -m pip install -r tools/requirements.txt
py -3.12 tools/tachie_studio.py
```

- `http://localhost:8787` が開いたら白背景画像をドロップする
- 自動透過後、濃色・市松・白背景を切り替えて輪郭を目視確認する
- 必要な箇所だけ魔法の杖・消しゴム・復元で修正し、PNG保存する
- 出力は `元ファイル名_clear.png`
- 完成素材は `立ち絵透過済み/` へ置く。参照パス変更は素材確認後に別作業で行う
- 離れた魔法・炎・輪・髪先を失わないよう「離れたパーツを保持」は通常ONにする

### フォルダ一括処理

```powershell
py -3.12 tools/prepare_tachie_batch.py 立ち絵AI生成 立ち絵透過下処理 --existing-dir 立ち絵透過済み
```

未処理の白背景立ち絵だけを抽出し、モデルを1回だけ読み込んで下処理する。
一括処理後も目視確認は必須。仕上げたPNGだけを `立ち絵透過済み/` へ移す。

### 注意事項
- 処理前に入力フォルダに立ち絵以外の画像(スクリーンショット等)が混ざっていないか確認する
- 元画像(`立ち絵AI生成/` 内)は削除・上書きしない。透過結果は必ず別フォルダに出す
- 白い服、細い髪、武器、独立エフェクト、足元の影を重点的に確認する
- WindowsではDirectML対応GPUを自動利用する。起動ログが `CPUExecutionProvider` の場合は1枚に数分かかることがある
- 詳細なパラメータ指針は `tools/TACHIE_STUDIO.md` を参照する
- 初回のみ `isnet-anime` モデル約176MBがユーザーフォルダへダウンロードされる

## コーディング方針

- 既存コードのスタイル(命名・構成)に合わせる。大規模なリファクタは事前に提案して承認を得る
- ビルドツール・フレームワークは導入しない(GitHub Pagesでそのまま動く構成を維持)
- UIを触るときはヴァイオレット×ゴールド×アンバーのテーマから外れない
- 日本語でコメント・会話する

## 背景の対応ルール

`game.js` の `BATTLE_DEFINITIONS` に登録する `background` は、
`scenario.js` でそのバトル直前のシーンに設定される `scene.bg` と一致させること。

例: プロローグのバトル直前シーンが `bg: "背景/オルクス魔王城鍛錬場.png"` なら、
`battle_tutorial` の `background` も `"背景/オルクス魔王城鍛錬場.png"` にする。

## このファイルの育て方(ユーザーへのメモ)

新しい道具や決まりごとができたら、このファイルに追記していくと
Claude Codeが毎回それを前提に動いてくれます。例:
- 新キャラ追加の手順(characters.jsへの登録フォーマット)
- 表情差分の命名規則(例: `ギュンター_呆れ.png` のように `キャラ名_表情.png`)
- 背景画像の追加手順
